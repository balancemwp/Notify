To create new Client Keys, use https://randomkeygen.com/
keys are based on CodeIgniter Encryption Keys

When in development, run smtp4dev in package manager to start the listener
to catch emails

If dev mode, check the appsettings Email:Dev mode.  If it set to true then run 
smtp4dev to see the emails.

@model Notify.Models.ClientMessage
    You have a request from @Model.Name.
    Their email is @Model.Email

    @if (Model.Phone != "")
    {
        <text>and phone # is </text> @Model.Phone;
    }

    Message: @Model.Message

